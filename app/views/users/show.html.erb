<% if current_user %>
    Welcome, <%= current_user.first_name %> <%= current_user.last_name %>!
<% end %>

<% current_user.notes.each do |note| %>
    <%= link_to "#{note.date} note for #{note.client.first_name}", client_note_path(note) %><br>
<% end %>